import type { LocaleManifest } from "./types";

// Stub — @vocoder/plugin replaces this at build time with the project's manifest.json content.
const manifest: LocaleManifest | null = null;
export default manifest;
